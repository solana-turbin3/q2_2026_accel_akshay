[package]
name = "serialization_challenge"
version = "0.1.0"
edition = "2024"

[dependencies]
borsh = {version="1.6.1", features = ["derive"]}
serde = {version="1.0.228", features = ["derive"]}
serde_json = "1.0.150"
wincode = {version="0.5.4", features = ["derive"]}
